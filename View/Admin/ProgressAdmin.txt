Progress en Funcionalidad de Admin:

    Autenticación:

        - Modificar controller de autenticación para permitir el ingreso de usuarios con el rol de admin. ☑

    Comunidades y Modificar Comunidades:

        - Tabla. ☑

        - Poblar vistas de modificación de comunidades dinámicamente. ☑
        - Update descripción. ☑
        
        - Update imagenes. ☒ (Método está funcionando bien, tiene chequeos de nulo para no sobreescribir los campos que se
                              dejan vacíos, lo único es que hay que ver como se va a hacer con el upload de archivos, eso
                              queda pendiente.) [OPCIONAL (Maybe?)]

    Usuarios:

        - Tabla. ☑

        - Update estado. ☒ (Requiere añadir columnas a la tabla. Para evitar enredos, queda pendiente. Por ahora el campo
                            en la tabla está comentado.) [NECESARIO]

        - Ver perfil. ☒ (Primero se necesitan las vistas de página de perfil.) [NECESARIO]

    Posts:

        - Tabla. ☑

        - Delete post. ☑

        - Ver cantidad de reportes. ☒ (Requiere añadir columnas a la tabla y cambiar otras cosas. Un poco tarde para      
                                       implementar esta funcionalidad, entonces queda pendiente.) [OPCIONAL]
                                       
        - Ver post. ☒ (Primero se necesitan las vistas de página de post.) [NECESARIO]
